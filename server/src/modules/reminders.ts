// Reminders feature removed.
import { Router } from 'express';
const router = Router();
export default router;
export function startReminderWorker() {}
export async function scheduleReminders(..._args: unknown[]) {}
